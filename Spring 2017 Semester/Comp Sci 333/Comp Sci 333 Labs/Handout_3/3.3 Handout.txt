3.3 Handout

printf("text");

"can use printf to print without using a type specifier"

core

"putty makes this file when there is a bug and it decides to dump all
the memory"


if (*min > x[j]) *min = x[j];

"You can do this if there is one statement executed"